<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= filename %></title>

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-black text-white min-h-screen flex items-center justify-center px-4">

    <div class="w-full max-w-4xl bg-zinc-900 rounded-xl shadow-lg p-6 space-y-4">

        <!-- Navigation -->
        <a href="/" class="text-blue-400 hover:text-blue-300 text-sm">
            ← Go to homepage
        </a>

        <!-- File Title -->
        <h1 class="text-3xl font-bold border-b border-zinc-700 pb-2">
            <%= filename %>
        </h1>

        <!-- File Content -->
        <div class="bg-black rounded-lg p-4 overflow-auto max-h-[60vh]">
            <pre class="text-sm text-zinc-200 whitespace-pre-wrap font-mono">
<%= filedata %>
            </pre>
        </div>

    </div>

</body>
</html>
    